# AGENTS.md

## 1. このファイルの目的

このリポジトリ `IEPpurchase` は、石川エンタープライズ向けの資材調達管理システムです。

Codex / AI 開発支援ツールは、実装・修正・レビューを行う際に、本ファイルを最優先の開発ルールとして扱ってください。

本プロジェクトでは、安全性を維持しつつ開発速度を優先します。

ユーザーへの確認は必要最小限とし、軽微・可逆的な技術判断についてはAI側で合理的に判断して実装を進めてください。

---

## 2. システム概要

本システムは、以下の業務を段階的に統合します。

1. 見積依頼受付（RFQ）
2. 見積依頼品目の管理
3. 仕入先候補の選定
4. 仕入先への見積依頼
5. Amazon / 楽天等の購入候補比較
6. 顧客向け販売見積作成
7. freee見積書連携
8. 発注・購入
9. 入荷・納品
10. 過去価格・販売実績・月次集計
11. メール自動取込
12. AI / OCR / 翻訳による自動化

Phase 1 では基盤機能のみを実装し、外部APIや完全自動化は実装しません。

---

## 3. 技術方針

原則として以下を使用します。

- Frontend: React
- Language: TypeScript
- Backend / DB: Firebase / Firestore
- Authentication: Firebase Authentication
- Hosting: Firebase Hosting を第一候補
- Source Control: Git / GitHub
- Editor: VS Code

既存プロジェクトの実装を最初に確認してください。

既存構成が上記と異なる場合でも、必要性がない限り大規模な構成変更は行わず、既存構成との整合性を優先してください。

軽微で可逆的な技術差異については、ユーザー確認を待たずAI側で適切な方法を選択して構いません。

大規模変更が必要な場合のみ、ユーザー確認対象とします。

---

## 4. AI判断とユーザー確認の基本原則

### 4.1 開発速度を優先する

本プロジェクトでは、ユーザーへの細かな確認を減らし、AI側の合理的な判断で開発を進めます。

以下の「確認必須事項」に該当しない場合、原則としてユーザーへ質問するために作業を停止しないでください。

複数の実装方法が存在する場合は、以下を優先してAI側で選択してください。

1. ユーザーの最新の明示的な指示
2. データを失わない
3. 既存機能を壊さない
4. 現在Phaseを超えない
5. 既存コード・既存設計との整合性
6. シンプルさ
7. 保守性
8. 将来変更しやすい構造

判断した内容は、必要に応じて作業終了時に簡潔に報告してください。

---

### 4.2 確認不要でAIが判断してよい事項

以下は原則としてユーザー確認不要です。

- UIの細かな配置
- UI文言の軽微な調整
- 余白や表示順
- コンポーネント分割
- 関数分割
- ファイル分割
- 型定義の追加・整理
- 既存命名規則に沿った命名
- TypeScriptエラー対応
- lintエラー対応
- buildエラー対応
- テストエラーの通常修正
- エラーハンドリング
- loading状態
- empty状態
- error状態
- 入力バリデーション
- 必須項目チェック
- 明らかなバグ修正
- 既存仕様を実現するための軽微な内部実装変更
- 既存パターンに沿った共通処理化
- 不要な重複コードの小規模整理
- Firestore document型の追加・修正
- 将来拡張を阻害しない軽微なFirestoreフィールド追加
- Firestore index追加
- server timestampの適用
- null / undefined処理
- 開発用ログの追加・整理
- テストコードの追加・修正
- README等の軽微な更新
- CURRENT_STATUS.mdの実装結果に伴う通常更新
- 管理シートへの通常の進捗反映
- 明らかに不要になった未使用import等の削除
- 既存UIパターンに沿ったレスポンシブ調整

これらについては、ユーザーへの確認よりも既存コードとの整合性を優先してください。

---

### 4.3 ユーザー確認が必要な事項

以下の場合のみ、原則として実装を停止してユーザーへ確認してください。

1. 要件が複数の意味に解釈でき、選択によって実際の業務フローが大きく変わる場合
2. 既存データの削除が必要な場合
3. 本番データの大量更新が必要な場合
4. 不可逆なデータ移行が必要な場合
5. 既存機能を削除する場合
6. 既存機能の主要な挙動を変更する場合
7. Firestoreの主要データモデルを全面的に変更する場合
8. Phaseを跨いだ実装が必要な場合
9. 外部サービスへの本番データ送信が必要な場合
10. 顧客・仕入先等への実メール送信が必要な場合
11. freee等への本番登録が必要な場合
12. 課金が発生するAPI・サービスを新規有効化する場合
13. 大量の課金API実行が必要な場合
14. Gitの破壊的操作が必要な場合
15. force pushが必要な場合
16. セキュリティ・認証・権限設計を大きく変更する場合
17. Security Rulesを大幅に緩和する場合
18. 要件定義書・管理シート・既存コード間に、実装結果を大きく左右する重大な矛盾がある場合
19. ユーザーの明示的な指示と既存仕様が明確に競合する場合
20. 外部公開・本番デプロイ等、ユーザーまたは第三者へ直接影響する操作を新たに行う場合

上記に該当しない場合は、質問のために作業を停止しないでください。

---

## 5. Phase開始時の包括承認

### 5.1 包括承認の考え方

ユーザーが特定のPhase、機能、または作業単位について実装開始を明示的に依頼した時点で、その対象範囲内の通常の実装判断について包括的に承認されたものとして扱います。

例:

- 「Phase 1を進めて」
- 「顧客マスターを実装して」
- 「次はRFQ登録を進めて」
- 「このPhaseを開始してください」

このような指示があった場合、対象機能を完成させるために必要となる軽微・可逆的な技術判断について、個別に確認を求める必要はありません。

---

### 5.2 包括承認に含まれるもの

包括承認には、対象機能の実装に必要な以下のような判断を含みます。

- コンポーネント追加
- ファイル追加
- 型定義追加
- Firestore document型追加
- 軽微なFirestoreフィールド追加
- 画面レイアウト
- フォーム構成
- バリデーション
- エラー表示
- loading表示
- empty表示
- 確認ダイアログ
- 一覧表示
- 検索
- 並び替え
- フィルタ
- 既存設計に沿ったCRUD実装
- helper / utility関数追加
- custom hook追加
- Firestore query実装
- Firestore index追加
- テスト追加
- lint / typecheck / build修正
- 動作確認に必要な軽微な周辺実装
- CURRENT_STATUS.md更新
- 管理シートの通常更新

これらについては、個別確認をせず実装を進めてください。

---

### 5.3 包括承認に含まれないもの

以下はPhase開始時の包括承認には含まれません。

- Phase外の機能
- 外部API連携の先行実装
- 本番環境への外部送信
- 顧客への実メール送信
- 仕入先への実メール送信
- freeeへの本番登録
- Amazon / 楽天等での実購入
- 本番データの大量削除
- Firestore主要構造の全面変更
- 課金APIの新規有効化
- force push
- git reset --hard
- 本番Security Rulesの大幅緩和
- 既存主要機能の削除

これらは個別確認を必要とします。

---

## 6. 開発時の絶対ルール

### 6.1 既存機能を壊さない

- 既存コードを最初に確認する。
- 不要な全面書き換えをしない。
- 大規模なリファクタリングは、明示的な依頼がない限り行わない。
- 変更範囲は必要最小限にする。
- 既存設計・既存命名・既存UIパターンを優先する。

既存仕様と新規実装に差異がある場合でも、業務フロー・保存データ・主要画面挙動に影響しない軽微な差異についてはAI側で判断してください。

重大な競合のみユーザーへ確認してください。

---

### 6.2 フェーズ単位で実装する

一度に複数Phaseを先取りして実装しないでください。

現在のPhaseは `docs/CURRENT_STATUS.md` および管理シートを確認してください。

Phase 1 対象外の例:

- 楽天API
- Amazon API / Product Advertising API
- freee API
- AI / OCR
- メール自動取込
- 発注自動化
- 完全自動翻訳

将来拡張を阻害しないデータ構造は許容します。

ただし、未依頼の外部連携は実装しないでください。

---

### 6.3 実装は「確認可能な機能単位」で行う

各機能は次の流れで進めます。

1. 現状確認
2. 実装計画を簡潔に提示
3. 確認必須事項がなければ、ユーザーの回答を待たず実装開始
4. 1つの確認可能な機能単位を実装
5. TypeScript / lint / build / tests等の自動チェック
6. 管理シートを `ユーザー確認待ち` に更新
7. ユーザー向け動作確認手順を提示
8. ユーザーが動作確認
9. ユーザーOK後、管理シートを `完了` に更新
10. 次の機能へ進む

---

### 6.4 「1機能」の定義

「1機能」は、画面上のボタン1個や入力欄1個ではなく、ユーザーが一度の動作確認で確認可能なまとまりとして扱います。

例:

- 顧客マスターCRUD一式
- 仕入先マスターCRUD一式
- RFQ登録一式
- RFQ Item管理一式
- 一覧検索・フィルタ一式
- 販売見積登録一式

例えば顧客マスターであれば、

- 一覧
- 新規登録
- 編集
- 削除
- バリデーション
- エラー処理

を必要に応じてまとめて実装して構いません。

これらを細分化して、都度ユーザー確認を求める必要はありません。

ただし、複数の大きな業務機能をまとめて先行実装してはいけません。

---

## 7. データ設計の原則

### 7.1 RFQ / RFQ Item を中心に管理

顧客見積依頼を RFQ とし、各品目を RFQ Item として管理します。

将来、以下は可能な限り `rfqId` / `rfqItemId` に紐付けます。

- supplierQuotes
- marketplaceOffers
- sourcingDecisions
- salesQuotes
- purchaseOrders
- deliveries
- documents
- activityLogs

---

### 7.2 Firebaseを自社側マスターデータの中心とする

freee、Amazon、楽天等の外部サービスを、本システムの唯一のマスターDBにしないでください。

外部サービスとの連携時は、必要に応じて以下のような外部IDを保存します。

- freeeQuotationId
- freeeDeliveryNoteId
- marketplaceOfferId
- externalDocumentId

---

### 7.3 履歴を消さない設計

見積価格、販売価格、仕入価格など、後から分析に使用する情報は、上書きのみで失わないようにします。

必要に応じて以下を使用してください。

- status
- revision
- createdAt
- updatedAt
- submittedAt
- archivedAt

価格や提出済み見積については、必要に応じてSnapshotとして保存できる構造を優先します。

---

### 7.4 軽微なデータ項目追加

現在Phaseの機能を実現するために必要で、既存構造を壊さず、将来拡張を阻害しない軽微なフィールド追加については、ユーザー確認不要です。

ただし、以下の場合はユーザー確認が必要です。

- 主要collection構造の変更
- 既存document構造の大規模変更
- 既存データ移行が必要
- 既存データを削除・変換する必要がある
- 複数業務機能に大きく影響する設計変更

---

## 8. 命名・コード品質

### TypeScript

- `any` の乱用禁止。
- 型定義を優先する。
- Firestore document 用の型を明示する。
- null / undefined の扱いを明確にする。
- 型安全を壊す回避実装を安易に追加しない。
- 既存型を利用できる場合は重複定義を避ける。

### 命名

- コンポーネント: PascalCase
- 関数・変数: camelCase
- 型・interface: PascalCase
- Firestore collection: camelCase 複数形を基本
- ID名: `customerId`, `supplierId`, `rfqId`, `rfqItemId`

### UI

- 業務システムとして、見た目より操作性・可読性を優先する。
- スマートフォン対応は意識する。
- Phase 1ではPCブラウザでの操作性を優先してよい。
- 一覧、検索、編集、状態確認を分かりやすくする。
- 既存UIパターンがある場合は、それに合わせる。
- UIの軽微な判断について個別確認は不要。

---

## 9. セキュリティ

### GitHubに絶対にコミットしないもの

以下は `.gitignore` 対象とし、ソースコードへ直書きしないでください。

- `.env`
- `.env.local`
- `.env.*.local`
- Firebase Admin秘密鍵
- Google Service Account JSON
- freee client_secret
- Amazon Secret
- 楽天 Secret
- メールパスワード
- APIキーのうち秘匿すべきもの
- private key
- certificate秘密鍵

Firebase Web API Key 等、公開前提の値についても、原則は環境変数で管理してください。

秘密情報を発見した場合は、Gitへ追加しないでください。

秘密情報をコード内に発見した場合は、作業終了報告等でユーザーへ明示してください。

秘密情報の削除や移動が既存環境へ影響する可能性がある場合は、必要に応じてユーザー確認対象とします。

---

## 10. Firebase / Firestore

- Firestore Security Rulesを無視しない。
- 開発目的で恒久的な全開放ルールを作らない。
- `createdAt`, `updatedAt` は原則 server timestamp を使用する。
- Firestoreクエリ数を不必要に増やさない。
- 大量データ前提の過剰最適化は不要。
- 想定規模は少人数（2〜5名）、月10〜100案件程度、1案件10品目前後。

この規模に適したシンプルな設計を優先します。

必要なindex追加や通常のquery調整はユーザー確認不要です。

---

## 11. 動作確認

各実装後、可能な範囲で以下を実行してください。

- TypeScript typecheck
- lint
- build
- relevant unit tests
- relevant integration tests

失敗した場合は、通常の修正で解決可能な範囲で自動的に修正してください。

lintやtypecheck等の通常エラー修正について、ユーザー確認は不要です。

実行できなかった場合は、理由を明記してください。

その後、ユーザーがブラウザで確認する項目を具体的に提示してください。

例:

1. `/customers` を開く
2. 「新規登録」を押す
3. 顧客名を入力
4. 保存
5. 一覧に表示されることを確認
6. 編集できることを確認
7. 再読込後も残ることを確認

---

## 12. Git運用

- 変更前に `git status` を確認する。
- 他作業の未コミット変更を勝手に消さない。
- `git reset --hard` 等の破壊的操作は、明示的な許可なしに行わない。
- force pushは明示的な許可なしに行わない。
- Phase / 機能ごとに意味のある単位でコミットする。
- コミットメッセージは内容が分かるものにする。
- ユーザーの動作確認前に、必要以上の大きなコミットをまとめない。

例:

- `feat: add customer master CRUD`
- `feat: add rfq item management`
- `fix: validate supplier form`
- `docs: update phase 1 status`

通常の `git status`, `git diff`, `git add`, `git commit` 等、破壊的でない操作については、現在の作業範囲内でAI側が実施して構いません。

ただし、ユーザーが別途Git運用方法を指定している場合は、その指示を優先してください。

---

## 13. Google Drive 開発資料・管理シート運用

本プロジェクトでは、Google Drive上の `IEPpurchase_開発管理シート` を正式な進捗管理記録として使用します。

Google Driveへアクセス可能な作業環境では、各作業の開始時・終了時に以下のルールを適用してください。

---

### 13.1 Google Drive 開発資料の参照先

このプロジェクトのGoogle Drive資料は `docs/DRIVE_RESOURCES.md` に定義します。

Google Drive開発資料フォルダ:

https://drive.google.com/drive/folders/1JCxWvh5D_KXSHpWfovhV9ZmELWAVGazO?usp=drive_link

進捗管理の正式な記録は、同フォルダ内の:

`IEPpurchase_開発管理シート`

とします。

---

### 13.2 作業開始時の確認順序

作業開始時は、原則として以下の順で確認してください。

1. `AGENTS.md`
2. `docs/CURRENT_STATUS.md`
3. `docs/DRIVE_RESOURCES.md`
4. `DRIVE_RESOURCES.md` に記載されたGoogle Drive開発資料フォルダ
5. `IEPpurchase_開発管理シート`
6. 今回の実装に関係する要件定義書・コード仕様書
7. `git status`
8. 現在のコード・プロジェクト構成

管理シートでは、最低限以下を確認してください。

- 現在Phase
- 完了済み機能
- ユーザー確認待ち項目
- 未解決事項
- 次に実装する内容
- 直近の変更・備考

---

### 13.3 読み込み量の最適化

管理資料を毎回全文読み込みする必要はありません。

以下を優先してください。

- 現在Phase
- 今回対象機能
- 未完了タスク
- ユーザー確認待ち
- 直近の完了タスク
- 未解決事項

要件定義書・コード仕様書も、今回の実装に関係する箇所のみ確認してください。

以下は避けてください。

- 毎回シート全体を再解析する
- Google Driveフォルダ全体を毎回全文解析する
- 同一チャット内ですでに確認済みの資料を理由なく再読込する
- CURRENT_STATUS.mdと管理シートで十分な場合に不要な資料を全文読む

---

### 13.4 作業中のステータス更新

実装およびCodex側の自動チェックが完了した時点で、管理シートの該当タスクを `ユーザー確認待ち` に更新して構いません。

この更新について個別のユーザー確認は不要です。

ただし、ユーザーの実際の動作確認が完了するまでは `完了` にしないでください。

---

### 13.5 作業終了時

ユーザーが動作確認して「OK」と回答した後、該当タスクを `完了` に更新してください。

完了時には、可能な範囲で以下を記録してください。

- ステータス
- 実装内容
- Codex確認結果
- ユーザー動作確認結果
- 確認日
- 不具合 / 修正内容
- commit hash
- 次の作業
- 更新日
- 備考

通常の進捗記録については、ユーザーへの個別確認は不要です。

---

### 13.6 管理情報の差異

管理シート、`docs/CURRENT_STATUS.md`、要件定義書、コード仕様書、既存コード間に差異がある場合でも、以下のような軽微な差異では作業を停止しないでください。

- 更新日の差
- 記述粒度の差
- 表現の差
- 名称の軽微な差
- 実装済みだが管理資料への反映が遅れている
- 実装結果に影響しない備考の差

これらは最新のユーザー指示と実際のコードを優先して判断し、必要に応じて作業終了時に報告してください。

以下のような重大な矛盾のみユーザーへ確認してください。

- 業務フローが変わる
- 保存するデータが変わる
- データ削除につながる
- 既存ユーザー操作が大きく変わる
- 現在Phaseが判断できない
- 実装対象そのものが異なる
- セキュリティ・権限仕様が競合する

---

### 13.7 Google Driveへアクセスできない場合

Google Driveへアクセスできない場合は:

- 管理シートを読んだことにしない。
- 管理シートを更新したことにしない。
- 推測で進捗を変更しない。
- アクセスできなかった旨をユーザーへ報告する。

ただし、Google Driveへアクセスできないことだけを理由に、ローカル資料・CURRENT_STATUS.md・既存コードから安全に判断できる実装まで停止する必要はありません。

現在Phaseや実装対象が明確で、重大な不確実性がない場合は、作業を継続して構いません。

---

### 13.8 Google Drive上の正式資料

以下はGoogle Drive側を正式資料として扱います。

- `IEPpurchase_開発管理シート`
- 資材調達管理システム 要件定義書
- 資材調達管理システム コード仕様書
- Phase実装プロンプト等の共有資料

明示的な依頼がない限り、要件定義書・コード仕様書そのものを変更しないでください。

---

## 14. 仕様確認の優先順位

仕様が矛盾・不明確な場合は、次の順で判断します。

1. ユーザーの最新の明示的な指示
2. 最新の管理シート
3. 最新の要件定義書
4. 最新のコード仕様書
5. `docs/ARCHITECTURE.md`
6. 既存コード
7. 過去の記述

この優先順位で合理的に解決できる場合は、ユーザーへ確認せず実装してください。

重大な判断が必要で、かつ確認必須事項に該当する場合のみユーザーへ確認してください。

軽微で可逆的な実装判断については、既存設計・命名規則に従ってAI側で判断してください。

---

## 15. 将来拡張で維持する考え方

以下を将来追加可能な構造にしてください。

- 米軍等からの見積依頼メール自動取込
- 添付PDF / Excel / 画像からの品目抽出
- 日英・英日翻訳
- メーカー・仕入先への自動見積依頼
- Amazon / 楽天価格比較
- freee見積書作成
- 提出済み見積のSnapshot / revision管理
- 発注
- 入荷
- 納品
- freee納品書連携
- 月別販売数量
- 品番別販売実績
- メーカー別販売実績
- 過去仕入価格 / 販売価格分析

Phase 1でこれら全てを実装する必要はありません。

将来対応可能なデータ構造にすることは許容しますが、現在Phaseで不要な機能を先行実装しないでください。

---

## 16. AIへの開始指示

新しい作業を始める際は、コード変更前に簡潔に以下を提示してください。

- 現在Phase
- 今回実装する機能
- 主な変更予定ファイル
- 注意事項・リスク（存在する場合のみ）
- ユーザー確認が必要な重大事項（存在する場合のみ）

前回から変更のない以下の内容は、毎回詳細に繰り返す必要はありません。

- プロジェクト全体構成
- 技術スタック
- 一般的な動作確認方法
- 既知の開発ルール
- Google Driveの場所
- 既に確認済みの要件全文

確認必須事項が存在しない場合、実装計画を提示した後にユーザーの返答を待つ必要はありません。

そのまま今回の対象機能を実装してください。

---

## 17. ユーザー確認待ち中の扱い

実装後にユーザーへ動作確認を依頼した場合、その機能は `ユーザー確認待ち` とします。

ユーザー確認待ち中は、原則として次の大きな業務機能へ進まないでください。

ただし、以下は継続して構いません。

- 同一機能内のバグ修正
- 同一機能内のUI調整
- 同一機能内のバリデーション修正
- 同一機能内のエラー処理修正
- 動作確認に必要な軽微な修正
- lint / typecheck / build対応
- テスト修正
- CURRENT_STATUS.md更新
- 管理シート更新

ユーザーが「OK」と回答した時点で、その機能を完了扱いにしてください。

---

## 18. 作業終了時の報告形式

各機能の実装後は、簡潔に以下を報告してください。

### 実装内容

今回変更した機能・主なファイル。

### 自動確認結果

- typecheck: 成功 / 失敗 / 未実施
- lint: 成功 / 失敗 / 未実施
- build: 成功 / 失敗 / 未実施
- tests: 成功 / 失敗 / 未実施

未実施の場合は理由を記載してください。

### ユーザー動作確認

ユーザーが実際に確認する手順を番号付きで示してください。

### 管理シート

以下のいずれかを明記してください。

- 更新済み / ユーザー確認待ち
- 完了更新済み
- Google Driveアクセス不可
- 更新不要

### 判断事項

AI側で判断した重要な軽微事項がある場合のみ簡潔に記載してください。

何もなければ省略して構いません。

### 次の作業

ユーザーのOK後に着手する次の1機能のみ記載してください。

---

## 19. 重要な禁止事項

以下は、ユーザーの明示的な許可なしに行わないでください。

- Phaseを跨いだ先行実装
- 既存主要機能の大規模削除
- データ構造の全面変更
- Firestore本番データの一括削除
- Firestore本番データの大規模・不可逆な変換
- Security Rulesの恒久的な全開放
- `git reset --hard`
- force push
- 秘密情報のGitコミット
- 外部サービスへの本番送信
- 顧客・仕入先への実メール送信
- freee等への本番登録
- Amazon / 楽天等での実購入
- 課金を伴うAPIの新規有効化
- 課金APIの大量実行
- 本番公開範囲の重大な変更
- 認証・権限モデルの大規模変更

必要な場合は、実行前に目的・影響・代替案を簡潔に説明し、ユーザーの確認を得てください。

---

## 20. 確認を増やさないための最終原則

本プロジェクトでは、確認のしすぎによる開発停止を避けてください。

判断に迷った場合でも、次の条件をすべて満たす場合は、ユーザーへ確認せずAI側で判断して進めてください。

- 現在Phase内である
- ユーザーが対象機能の実装を承認済みである
- 既存データを失わない
- 既存主要機能を壊さない
- セキュリティを弱めない
- 外部への本番送信を行わない
- 課金を発生させない
- Gitの破壊的操作を行わない
- 後から容易に修正できる
- 既存設計との整合性がある

この条件を満たす場合、「念のため確認する」ことを理由に作業を停止しないでください。

ユーザーへの確認は、実装結果や業務仕様を大きく左右する事項に限定してください。

本プロジェクトでは、

「細かな技術判断はAIに任せ、業務上重要な判断だけユーザーが行う」

ことを基本方針とします。
