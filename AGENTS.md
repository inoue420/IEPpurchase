# AGENTS.md

## 1. このファイルの目的

このリポジトリ `IEPpurchase` は、石川エンタープライズ向けの資材調達管理システムです。

Codex / AI 開発支援ツールは、実装・修正・レビューを行う際に、本ファイルを最優先の開発ルールとして扱ってください。

---

## 2. システム概要

本システムは、以下の業務を段階的に統合します。

1. 見積依頼受付（RFQ）
2. 見積依頼品目の管理
3. 仕入先候補の選定
4. 仕入先への見積依頼
5. Amazon / 楽天等の購入候補比較
6. 顧客向け販売見積作成
7. freee見積書連携
8. 発注・購入
9. 入荷・納品
10. 過去価格・販売実績・月次集計
11. メール自動取込
12. AI / OCR / 翻訳による自動化

Phase 1 では基盤機能のみを実装し、外部APIや完全自動化は実装しません。

---

## 3. 技術方針

原則として以下を使用します。

- Frontend: React
- Language: TypeScript
- Backend / DB: Firebase / Firestore
- Authentication: Firebase Authentication
- Hosting: Firebase Hosting を第一候補
- Source Control: Git / GitHub
- Editor: VS Code

既存プロジェクトの実装を最初に確認し、既存構成が上記と異なる場合は、勝手に大規模変更せず、必要性を説明してから変更してください。

---

## 4. 開発時の絶対ルール

### 4.1 既存機能を壊さない

- 既存コードを最初に確認する。
- 不要な全面書き換えをしない。
- 大規模なリファクタリングは、明示的な依頼がない限り行わない。
- 変更範囲は必要最小限にする。
- 既存仕様と新規実装が競合する場合は、勝手に判断せず差異を報告する。

### 4.2 フェーズ単位で実装する

一度に複数Phaseを先取りして実装しないでください。

現在のPhaseは `docs/CURRENT_STATUS.md` を確認してください。

Phase 1 対象外の例:

- 楽天API
- Amazon API / Product Advertising API
- freee API
- AI / OCR
- メール自動取込
- 発注自動化
- 完全自動翻訳

将来拡張を阻害しないデータ構造は許容しますが、未依頼の外部連携を実装しないでください。

### 4.3 実装は小さな単位で行う

各機能は次の流れで進めます。

1. 現状確認
2. 実装計画提示
3. 1機能のみ実装
4. TypeScript / lint / build 等の自動チェック
5. ユーザー向け動作確認手順提示
6. ユーザーが動作確認
7. OK後に次の機能へ進む
8. Phase終了時に管理情報を更新

ユーザーの動作確認前に、勝手に次の大きな実装へ進まないでください。

---

## 5. データ設計の原則

### 5.1 RFQ / RFQ Item を中心に管理

顧客見積依頼を RFQ とし、各品目を RFQ Item として管理します。

将来、以下は可能な限り `rfqId` / `rfqItemId` に紐付けます。

- supplierQuotes
- marketplaceOffers
- sourcingDecisions
- salesQuotes
- purchaseOrders
- deliveries
- documents
- activityLogs

### 5.2 Firebaseを自社側マスターデータの中心とする

freee、Amazon、楽天等の外部サービスを、本システムの唯一のマスターDBにしないでください。

外部サービスとの連携時は、必要に応じて以下のような外部IDを保存します。

- freeeQuotationId
- freeeDeliveryNoteId
- marketplaceOfferId
- externalDocumentId

### 5.3 履歴を消さない設計

見積価格、販売価格、仕入価格など、後から分析に使用する情報は、上書きのみで失わないようにします。

必要に応じて以下を使用してください。

- status
- revision
- createdAt
- updatedAt
- submittedAt
- archivedAt

価格や提出済み見積については、必要に応じてSnapshotとして保存できる構造を優先します。

---

## 6. 命名・コード品質

### TypeScript

- `any` の乱用禁止。
- 型定義を優先する。
- Firestore document 用の型を明示する。
- null / undefined の扱いを明確にする。
- 型安全を壊す回避実装を安易に追加しない。

### 命名

- コンポーネント: PascalCase
- 関数・変数: camelCase
- 型・interface: PascalCase
- Firestore collection: camelCase 複数形を基本
- ID名: `customerId`, `supplierId`, `rfqId`, `rfqItemId`

### UI

- 業務システムとして、見た目より操作性・可読性を優先する。
- スマートフォン対応は意識するが、Phase 1ではPCブラウザでの操作性を優先してよい。
- 一覧、検索、編集、状態確認を分かりやすくする。
- 既存UIパターンがある場合は、それに合わせる。

---

## 7. セキュリティ

### GitHubに絶対にコミットしないもの

以下は `.gitignore` 対象とし、ソースコードへ直書きしないでください。

- `.env`
- `.env.local`
- `.env.*.local`
- Firebase Admin秘密鍵
- Google Service Account JSON
- freee client_secret
- Amazon Secret
- 楽天 Secret
- メールパスワード
- APIキーのうち秘匿すべきもの
- private key
- certificate秘密鍵

Firebase Web API Key 等、公開前提の値についても、原則は環境変数で管理してください。

秘密情報を発見した場合は、勝手にGitへ追加せずユーザーへ報告してください。

---

## 8. Firebase / Firestore

- Firestore Security Rulesを無視しない。
- 開発目的で恒久的な全開放ルールを作らない。
- `createdAt`, `updatedAt` は原則 server timestamp を使用する。
- Firestoreクエリ数を不必要に増やさない。
- 大量データ前提の過剰最適化は不要。
- 想定規模は少人数（2〜5名）、月10〜100案件程度、1案件10品目前後。

この規模に適したシンプルな設計を優先します。

---

## 9. 動作確認

各実装後、可能な範囲で以下を実行してください。

- TypeScript typecheck
- lint
- build
- relevant unit tests
- relevant integration tests

実行できなかった場合は、理由を明記してください。

その後、ユーザーがブラウザで確認する項目を具体的に提示してください。

例:

1. `/customers` を開く
2. 「新規登録」を押す
3. 顧客名を入力
4. 保存
5. 一覧に表示されることを確認
6. 再読込後も残ることを確認

---

## 10. Git運用

- 変更前に `git status` を確認する。
- 他作業の未コミット変更を勝手に消さない。
- `git reset --hard` 等の破壊的操作は、明示的な許可なしに行わない。
- Phase / 機能ごとに意味のある単位でコミットする。
- コミットメッセージは内容が分かるものにする。
- ユーザーの動作確認前に、必要以上の大きなコミットをまとめない。

例:

- `feat: add customer master CRUD`
- `feat: add rfq item management`
- `fix: validate supplier form`
- `docs: update phase 1 status`

---

## 11. Google Drive 開発資料・管理シート運用

本プロジェクトでは、Google Drive上の `IEPpurchase_開発管理シート` を正式な進捗管理記録として使用します。

Google Driveへアクセス可能な作業環境では、各作業の開始時・終了時に以下のルールを必ず適用してください。

### 11.1 Google Drive 開発資料の参照先

このプロジェクトのGoogle Drive資料は `docs/DRIVE_RESOURCES.md` に定義します。

Google Drive開発資料フォルダ:

https://drive.google.com/drive/folders/1JCxWvh5D_KXSHpWfovhV9ZmELWAVGazO?usp=drive_link

進捗管理の正式な記録は、同フォルダ内の:

`IEPpurchase_開発管理シート`

とします。

### 11.2 作業開始時の確認順序

作業開始時は、必ず以下の順で確認してください。

1. `AGENTS.md`
2. `docs/CURRENT_STATUS.md`
3. `docs/DRIVE_RESOURCES.md`
4. `DRIVE_RESOURCES.md` に記載されたGoogle Drive開発資料フォルダ
5. `IEPpurchase_開発管理シート`
6. 今回の実装に関係する要件定義書・コード仕様書
7. `git status`
8. 現在のコード・プロジェクト構成

管理シートでは、最低限以下を確認してください。

- 現在Phase
- 完了済み機能
- ユーザー確認待ち項目
- 未解決事項
- 次に実装する内容
- 直近の変更・備考

### 11.3 読み込み量の最適化

- 管理シートは、現在Phase、未完了タスク、直近の完了タスクを優先して確認する。
- 毎回シート全体を再解析する必要はない。
- 要件定義書・コード仕様書も、今回の実装に関係する箇所のみ確認する。
- 同一チャット内で既に確認済みの資料を、理由なく繰り返し全文読み込みしない。
- Google Driveフォルダ全体を毎回全文解析しない。
- 必要な情報が管理シート・CURRENT_STATUS.mdで十分な場合は、不要な資料再読込を避ける。

### 11.4 作業中のステータス更新

実装およびCodex側の自動チェックが完了した時点で、管理シートの該当タスクを `ユーザー確認待ち` に更新してよいものとします。

ユーザーの実際の動作確認が完了するまでは、`完了` にしないでください。

### 11.5 作業終了時

ユーザーが動作確認して「OK」と回答した後に、該当タスクを `完了` に更新してください。

完了時には、可能な範囲で以下を記録してください。

- ステータス
- 実装内容
- Codex確認結果
- ユーザー動作確認結果
- 確認日
- 不具合 / 修正内容
- commit hash
- 次の作業
- 更新日
- 備考

### 11.6 管理情報の矛盾

管理シートの内容と、リポジトリ内 `docs/CURRENT_STATUS.md` が矛盾する場合は、勝手に判断せず差異を報告してください。

### 11.7 Google Driveへアクセスできない場合

Google Driveへアクセスできない場合は:

- 管理シートを読んだことにしない。
- 管理シートを更新したことにしない。
- 推測で進捗を変更しない。
- アクセスできなかった旨をユーザーへ報告する。

### 11.8 Google Drive上の正式資料

以下はGoogle Drive側を正式資料として扱います。

- `IEPpurchase_開発管理シート`
- 資材調達管理システム 要件定義書
- 資材調達管理システム コード仕様書
- Phase実装プロンプト等の共有資料

明示的な依頼がない限り、要件定義書・コード仕様書そのものを変更しないでください。

---

## 12. 仕様確認の優先順位

仕様が矛盾・不明確な場合は、次の順で確認します。

1. ユーザーの最新の明示的な指示
2. 最新の管理シート
3. 最新の要件定義書
4. 最新のコード仕様書
5. `docs/ARCHITECTURE.md`
6. 既存コード
7. 過去の記述

重大な判断が必要な場合は、推測で実装せずユーザーへ確認してください。

軽微で可逆的な実装判断については、既存設計・命名規則に従い、必要に応じて判断理由を報告してください。

---

## 13. 将来拡張で維持する考え方

以下を将来追加可能な構造にしてください。

- 米軍等からの見積依頼メール自動取込
- 添付PDF / Excel / 画像からの品目抽出
- 日英・英日翻訳
- メーカー・仕入先への自動見積依頼
- Amazon / 楽天価格比較
- freee見積書作成
- 提出済み見積のSnapshot / revision管理
- 発注
- 入荷
- 納品
- freee納品書連携
- 月別販売数量
- 品番別販売実績
- メーカー別販売実績
- 過去仕入価格 / 販売価格分析

Phase 1でこれら全てを実装する必要はありません。

---

## 14. AIへの開始指示

新しい作業を始める際は、コード変更前に必ず以下を提示してください。

- 現在のプロジェクト構成の確認結果
- 現在Phase
- 今回実装する内容
- 変更予定ファイル
- 推奨実装順序
- 最初の動作確認方法
- 想定されるリスク
- 現時点でユーザー確認が必要な問題

重大な確認事項がなければ、その後、今回合意した1機能のみ実装してください。

---

## 15. 作業終了時の報告形式

各機能の実装後は、簡潔に以下を報告してください。

### 実装内容
今回変更した機能・ファイル。

### 自動確認結果
- typecheck
- lint
- build
- tests

各結果を成功 / 失敗 / 未実施で示す。

### ユーザー動作確認
ユーザーが実際に確認する手順を番号付きで示す。

### 管理シート
- 更新済み
- ユーザー確認待ち
- Google Driveアクセス不可

のいずれかを明記する。

### 次の作業
ユーザーのOK後に着手する次の1機能のみ記載する。

---

## 16. 重要な禁止事項

以下は、ユーザーの明示的な許可なしに行わないでください。

- Phaseを跨いだ先行実装
- 既存機能の大規模削除
- データ構造の全面変更
- Firestore本番データの一括削除
- Security Rulesの恒久的な全開放
- `git reset --hard`
- 強制push
- 秘密情報のGitコミット
- 外部サービスへの本番送信
- 顧客・仕入先への実メール送信
- freee等への本番登録
- 課金を伴うAPIの有効化・大量実行

必要な場合は、実行前に目的・影響・代替案を説明し、ユーザーの確認を得てください。
