# AGENTS.md

## 1. 目的

このリポジトリ `IEPpurchase` は、石川エンタープライズ向けの資材調達管理システムです。

Codex / AI 開発支援ツールは、実装・修正・レビュー・原因調査を行う際に、本ファイルを最優先の開発ルールとして扱ってください。

本プロジェクトでは、

- 既存機能を壊さない
- データを失わない
- 現在Phaseを超えない
- ユーザー確認を必要最小限にする
- 軽微な技術判断はAI側で行う
- sandbox制約による不要な作業停止を避ける

ことを基本方針とします。

---

# 2. 最優先実行原則

本章は、本ファイル内の他の一般的な運用ルールより優先します。

1. リポジトリ内の読み取り・検索・調査はユーザー承認不要です。
2. ソースコード、設定ファイル、Git状態、関連ドキュメントは必要に応じて自律的に確認してください。
3. ユーザーから実装を依頼された場合、対象機能内の通常の技術判断は包括承認済みとして扱います。
4. 読み取り許可は、Codexのsandbox・OS・接続サービス等の技術的権限を拡張するものではありません。
5. 現在利用可能なworkspace / sandbox内で実行可能な方法を優先してください。
6. 1つのコマンド・ファイル・外部資料へのアクセス失敗だけで、作業全体を停止しないでください。
7. 非破壊的な代替手段がある場合は、ユーザー確認なしで試してください。
8. Google Drive等の外部資料へアクセスできなくても、ローカル資料と既存コードで判断可能なら作業を継続してください。
9. sandbox外実行、権限昇格、保護領域への無理なアクセスは行わないでください。
10. 補助的な確認やテストが実行不能でも、本体実装が可能なら継続し、未実施事項を終了時に報告してください。
11. 「念のため確認する」ことを理由にユーザーへの質問や作業停止を増やさないでください。
12. ユーザー確認は、業務仕様・本番環境・データ・課金・外部送信等に重大な影響がある場合に限定してください。

---

# 3. システム概要

本システムは、以下の業務を段階的に統合します。

1. 見積依頼受付（RFQ）
2. 見積依頼品目管理
3. 仕入先候補選定
4. 仕入先への見積依頼
5. Amazon / 楽天等の購入候補比較
6. 顧客向け販売見積作成
7. freee見積書連携
8. 発注・購入
9. 入荷・納品
10. 過去価格・販売実績・月次集計
11. メール自動取込
12. AI / OCR / 翻訳

Phase 1では基盤機能のみを実装し、外部API・完全自動化は実装しません。

---

# 4. 技術方針

原則として以下を使用します。

- Frontend: React
- Language: TypeScript
- Backend / DB: Firebase / Firestore
- Authentication: Firebase Authentication
- Hosting: Firebase Hosting
- Source Control: Git / GitHub
- Editor: VS Code

既存プロジェクトの構成を優先してください。

既存構成が上記と多少異なる場合でも、必要性がない限り大規模変更は行わないでください。

---

# 5. Phase開始時の包括承認

## 5.1 包括承認

ユーザーが以下のような実装開始指示を出した時点で、

- 「Phase 1を進めて」
- 「顧客マスターを実装して」
- 「次の機能を進めて」
- 「RFQ登録をお願いします」

その対象機能を完成させるための通常の技術判断は包括的に承認されたものとして扱います。

個別の承認を求める必要はありません。

---

## 5.2 包括承認に含まれるもの

以下は原則としてユーザー確認不要です。

- ソースコードの確認
- ファイル・ディレクトリ検索
- コンポーネント追加
- ファイル追加
- 型定義追加
- helper / hook / utility追加
- 既存設計に沿ったCRUD
- UI配置
- UI文言の軽微な調整
- バリデーション
- loading / empty / error表示
- エラーハンドリング
- 確認ダイアログ
- 検索・並び替え・フィルタ
- TypeScript修正
- lint修正
- build修正
- テスト修正
- 明らかなバグ修正
- 軽微なFirestoreフィールド追加
- Firestore index追加
- server timestamp適用
- CURRENT_STATUS.md更新
- 管理シートへの通常の進捗記録
- 非破壊的なGit確認
- 通常のgit add / git commit

複数の実装方法がある場合は、既存コードとの整合性、シンプルさ、保守性を優先してAI側で選択してください。

---

# 6. ソース・リポジトリ確認の許可

## 6.1 常時許可される読み取り操作

以下は常時承認済みです。

- ファイルを開く
- ソースコードを読む
- ファイル一覧取得
- ディレクトリ確認
- 文字列検索
- シンボル検索
- import / export確認
- 呼び出し元確認
- 参照元確認
- package.json確認
- tsconfig確認
- Firebase設定確認
- Firestore Rules確認
- Firestore index確認
- `.gitignore` 確認
- README / docs確認
- AGENTS.md確認
- CURRENT_STATUS.md確認
- DRIVE_RESOURCES.md確認
- 既存テスト確認
- Git履歴確認

例:

- `git status`
- `git diff`
- `git diff --staged`
- `git log`
- `git show`
- `git branch`
- `git remote -v`
- `rg`
- `grep`
- `find`
- `dir`
- `ls`
- `cat`
- `type`

これらについてユーザーへ許可を求めてはいけません。

---

## 6.2 対象ファイルが不明な場合

対象箇所が不明でも、最初にユーザーへファイル場所を質問しないでください。

原則として、

1. 機能名・画面名・型名・文字列を検索
2. 関係ファイルを確認
3. import / 呼び出し元を追跡
4. 関連するcomponent / service / hook / typeを確認
5. 必要ならGit履歴を確認

の順で自律的に対象箇所を特定してください。

---

# 7. sandbox / access error 時の処理

コマンド・ファイル・外部サービス等で、

- sandbox error
- permission denied
- access denied
- unavailable
- file not found
- connection unavailable

等が発生しても、直ちに作業全体を停止しないでください。

まず以下を確認してください。

1. パスが正しいか
2. workspace内の相対パスでアクセスできないか
3. 別の非破壊的な読み取り方法がないか
4. 別コマンドで代替できないか
5. 今回の作業に本当に必須か
6. ローカル資料で代替可能か

---

## 7.1 sandboxエラー時の禁止事項

1回のアクセス失敗だけで、

- 「sandboxが開けない」
- 「リポジトリへアクセスできない」
- 「作業を継続できない」

と判断しないでください。

別の安全な方法がある場合は自律的に試してください。

---

## 7.2 権限を無理に広げない

本ファイルの「包括許可」は、Codexのsandbox権限そのものを変更する指示ではありません。

以下は行わないでください。

- sandbox外への無理なアクセス
- OS権限昇格
- 管理者権限取得
- 保護領域への強制アクセス
- sandbox制約を回避するための危険な操作

現在付与されている権限内で作業してください。

---

## 7.3 補助処理が失敗した場合

例えば、

- Git commitがsandbox制約でできない
- buildだけ実行できない
- test runnerだけ起動できない
- Google Driveだけ開けない

場合、その処理だけを未実施として扱ってください。

他の実装・確認が可能なら作業を継続してください。

---

# 8. 作業開始時の確認順序

原則として以下の順で確認してください。

1. `AGENTS.md`
2. `docs/CURRENT_STATUS.md`
3. `git status`
4. 今回対象となるコード・プロジェクト構成
5. `docs/DRIVE_RESOURCES.md`
6. 必要な場合のみGoogle Drive管理シート
7. 必要な場合のみ要件定義書・コード仕様書

Google Driveを確認するために、ローカルソース確認を遅らせないでください。

---

# 9. Google Drive資料

Google Drive上の以下を正式資料として扱います。

- `IEPpurchase_開発管理シート`
- 資材調達管理システム 要件定義書
- 資材調達管理システム コード仕様書
- Phase実装プロンプト等

Google Drive資料フォルダは `docs/DRIVE_RESOURCES.md` に定義します。

---

## 9.1 Google Driveは毎回必須ではない

以下から今回の作業内容を安全に判断できる場合、

- ユーザーの最新指示
- CURRENT_STATUS.md
- 既存コード
- Git状態

Google Driveを毎回確認する必要はありません。

管理シートや正式要件の確認が必要な場合のみアクセスしてください。

---

## 9.2 Google Driveへアクセスできない場合

アクセス不能時は、

- 読んだことにしない
- 更新したことにしない
- 推測で管理情報を書き換えない

でください。

ただし、現在Phaseと実装内容が十分明確なら、Google Driveアクセス不能だけを理由にコード作業を停止しないでください。

終了時に、

`Google Driveアクセス不可`

と報告してください。

---

## 9.3 読み込み量

毎回Drive全体・管理シート全体・要件書全文を再解析する必要はありません。

今回の作業に関係する範囲のみ確認してください。

---

# 10. 実装単位

一度に複数Phaseを先取りしないでください。

「1機能」は、ユーザーが一度の動作確認で確認できるまとまりとします。

例:

- 顧客マスターCRUD
- 仕入先マスターCRUD
- RFQ登録
- RFQ Item管理
- 一覧検索・フィルタ

例えば顧客マスターであれば、

- 一覧
- 新規
- 編集
- 削除
- バリデーション
- エラー処理

を一式として実装して構いません。

ボタンや入力欄単位でユーザー確認を求める必要はありません。

---

# 11. 開発フロー

各機能は以下の流れで進めます。

1. 必要な資料・ソースを承認不要で確認
2. 現状と対象機能を特定
3. 必要なら実装方針を簡潔に報告
4. 重大な確認事項がなければ返答を待たず実装
5. 1機能を完成
6. typecheck / lint / build / testsを可能な範囲で実行
7. 管理シートを可能なら `ユーザー確認待ち` に更新
8. ユーザー向け動作確認手順を提示
9. ユーザー確認
10. OK後に `完了` 更新
11. 次の機能へ進む

---

# 12. ユーザー確認が必要な事項

以下の場合のみ、原則として実装を停止して確認してください。

1. 業務フローが大きく変わる
2. 既存データを削除する
3. 本番データを大量更新する
4. 不可逆なデータ移行を行う
5. 既存主要機能を削除する
6. 主要機能の挙動を大幅変更する
7. Firestore主要データモデルを全面変更する
8. Phaseを跨ぐ
9. 外部サービスへ本番データ送信する
10. 顧客・仕入先へ実メール送信する
11. freee等へ本番登録する
12. Amazon / 楽天等で実購入する
13. 課金APIを新規有効化する
14. 大量の課金APIを実行する
15. `git reset --hard` を行う
16. force pushする
17. Security Rulesを大幅に緩和する
18. 認証・権限モデルを大幅変更する
19. 本番公開範囲を大きく変更する
20. 正式仕様間に重大な矛盾があり、実装結果が大きく変わる

これ以外では、原則として質問のために作業を停止しないでください。

---

# 13. 既存機能保護

- 既存コードを確認してから変更する
- 不要な全面書き換えをしない
- 明示依頼なしの大規模リファクタリングをしない
- 変更範囲を必要最小限にする
- 他作業の未コミット変更を消さない
- 既存UI・命名・設計パターンを優先する

軽微な差異はAI側で判断してください。

---

# 14. Phase制限

現在Phaseは `docs/CURRENT_STATUS.md` を基準に確認します。

Phase 1では原則として以下を実装しません。

- 楽天API
- Amazon API
- freee API
- AI / OCR
- メール自動取込
- 発注自動化
- 完全自動翻訳

将来拡張可能なデータ構造は許容します。

ただし、未依頼機能を先行実装しないでください。

---

# 15. データ設計

## 15.1 RFQ中心設計

顧客見積依頼をRFQ、各品目をRFQ Itemとして管理します。

将来以下は可能な限り `rfqId` / `rfqItemId` に紐付けます。

- supplierQuotes
- marketplaceOffers
- sourcingDecisions
- salesQuotes
- purchaseOrders
- deliveries
- documents
- activityLogs

---

## 15.2 Firebaseを自社側マスターとする

freee、Amazon、楽天等を本システムの唯一のマスターDBにしないでください。

必要に応じて外部IDを保存します。

例:

- freeeQuotationId
- freeeDeliveryNoteId
- marketplaceOfferId
- externalDocumentId

---

## 15.3 履歴を失わない

見積価格、販売価格、仕入価格等は、後から分析できるよう履歴を失わない設計にしてください。

必要に応じて以下を使用します。

- status
- revision
- createdAt
- updatedAt
- submittedAt
- archivedAt
- snapshot

---

# 16. TypeScript / コード品質

- `any` の乱用禁止
- 型定義を優先
- Firestore document型を明示
- null / undefinedを明確に扱う
- 型安全を壊す回避実装を避ける
- 既存型がある場合は再利用する

命名:

- Component: PascalCase
- 関数・変数: camelCase
- 型/interface: PascalCase
- Firestore collection: camelCase複数形
- ID: `customerId`, `supplierId`, `rfqId`, `rfqItemId`

---

# 17. UI方針

- 見た目より操作性・可読性を優先
- Phase 1はPCブラウザ優先
- スマートフォン対応も意識
- 一覧・検索・編集・状態確認を分かりやすくする
- 既存UIパターンに合わせる

軽微なUI判断はユーザー確認不要です。

---

# 18. Firebase / Firestore

- Security Rulesを無視しない
- 恒久的な全開放ルールを作らない
- `createdAt`, `updatedAt` は原則server timestamp
- 不要なFirestoreクエリを増やさない
- 過剰最適化しない

想定規模:

- 利用者: 2〜5名
- 月間案件: 10〜100件程度
- 1案件: 約10品目

この規模に適したシンプルな構造を優先してください。

---

# 19. セキュリティ

以下をGitへコミットしないでください。

- `.env`
- `.env.local`
- `.env.*.local`
- Firebase Admin秘密鍵
- Google Service Account JSON
- freee client_secret
- Amazon Secret
- 楽天 Secret
- メールパスワード
- private key
- certificate秘密鍵
- その他秘匿情報

Firebase Web API Key等も原則環境変数で管理します。

秘密情報を発見しても、必要なく値を表示・転記しないでください。

---

# 20. 自動確認

実装後、可能な範囲で以下を実行してください。

- TypeScript typecheck
- lint
- build
- relevant unit tests
- relevant integration tests

通常のエラーはユーザー確認なしで修正してください。

sandbox制約等で実行不能なものは、

`未実施: 理由`

として報告し、他の作業が可能なら継続してください。

自動チェックが1つ失敗・実行不能であることだけを理由に、作業全体を停止しないでください。

---

# 21. Git運用

変更前に `git status` を確認してください。

非破壊的操作はユーザー確認不要です。

例:

- git status
- git diff
- git log
- git show
- git branch
- git remote -v
- git add
- 通常のgit commit

禁止:

- `git reset --hard`
- force push
- 他作業の未コミット変更削除
- 秘密情報のcommit

上記禁止操作は明示許可が必要です。

Git書き込みがsandbox制約で失敗した場合は、commitを省略して実装・検証を続行し、終了時に報告してください。

---

# 22. 管理シート

実装と自動確認が完了した時点で、アクセス可能なら該当タスクを

`ユーザー確認待ち`

へ更新して構いません。

ユーザーが実際に「OK」と回答するまでは `完了` にしないでください。

OK後に可能な範囲で以下を記録します。

- ステータス
- 実装内容
- 自動確認結果
- ユーザー確認結果
- 確認日
- 不具合・修正内容
- commit hash
- 次の作業
- 更新日
- 備考

---

# 23. 仕様確認の優先順位

仕様に差異がある場合は以下を優先します。

1. ユーザーの最新指示
2. 最新の管理シート
3. 最新の要件定義書
4. 最新のコード仕様書
5. `docs/ARCHITECTURE.md`
6. 既存コード
7. 過去の記述

この優先順位で合理的に解決できる場合は、ユーザーへ確認せず進めてください。

---

# 24. 軽微な資料差異

以下の差異では作業を停止しないでください。

- 更新日の差
- 表現の差
- 記述粒度の差
- 軽微な名称差
- 実装済みだが管理表反映が遅れている
- 実装結果に影響しない備考差

重大な業務仕様・データ仕様の矛盾のみユーザー確認対象です。

---

# 25. 将来拡張

将来以下を追加可能な構造を維持してください。

- メール自動取込
- PDF / Excel / 画像からの品目抽出
- AI / OCR
- 翻訳
- メーカー・仕入先への自動見積依頼
- Amazon / 楽天価格比較
- freee見積作成
- 見積revision / snapshot
- 発注
- 入荷
- 納品
- freee納品書連携
- 月別販売数量
- 品番別販売実績
- メーカー別販売実績
- 過去仕入価格 / 販売価格分析

現在Phaseでは必要な範囲のみ実装してください。

---

# 26. 作業開始時の報告

コード変更前の報告は簡潔で構いません。

必要に応じて以下のみ示してください。

- 現在Phase
- 今回の実装機能
- 主な変更予定ファイル
- 重大なリスク
- ユーザー確認が必要な重大事項

重大事項がなければ、ユーザーの返答を待たずそのまま実装してください。

毎回以下を繰り返す必要はありません。

- 技術スタック
- プロジェクト全体構成
- Google Driveの場所
- 既知の開発ルール
- 既に読んだ要件全文

---

# 27. 作業終了時の報告

各機能の終了時は簡潔に以下を報告してください。

## 実装内容

変更した機能・主なファイル。

## 自動確認結果

- typecheck: 成功 / 失敗 / 未実施
- lint: 成功 / 失敗 / 未実施
- build: 成功 / 失敗 / 未実施
- tests: 成功 / 失敗 / 未実施

## ユーザー動作確認

ブラウザで確認する手順を番号付きで提示。

## 管理シート

以下のいずれか。

- 更新済み / ユーザー確認待ち
- 完了更新済み
- Google Driveアクセス不可
- 更新不要

## 次の作業

ユーザーOK後に進める次の1機能のみ記載。

---

# 28. 最終原則

本プロジェクトでは、

**読む・検索する・調査する・非破壊的に確認する操作は常時許可**

**現在Phase内の軽微な技術判断はAIに委任**

**sandboxエラーが1つ発生しても、代替手段やローカル情報で継続可能なら作業を続ける**

**Google Driveアクセス不能だけで実装を停止しない**

**業務仕様・データ・本番環境・課金・外部送信・破壊的操作に重大な影響がある場合だけユーザーへ確認する**

ことを基本方針とします。

「念のため確認する」ことより、

**安全な範囲で自律的に調査・判断・実装を進めること**

を優先してください。
