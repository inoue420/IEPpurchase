# IEP-P3-005 翻訳明細付きfreee見積書の帳票・一連動作確認

2026-09-14。自動検証と受入手順を整備。実freee登録・帳票表示はユーザー確認待ち。

## 対象
正式管理シートのP3-005に従い、翻訳確認済み明細から保存・転記・結果・履歴までを検証する。新規の業務機能や自動送信は追加しない。

## 自動検証
- tests/firestore/freeeQuotation.integration.test.ts に、長文（品番と区切りスペース込み256文字）・登録英訳なしの未確認状態で保存とHTTPを実行しないテストを追加。
- 複数品目、改行・Unicode・先頭ゼロ品番、小数数量、混在税率、訳文なしの手動確認、RFQ原文変更後の固定Snapshot、HTTPへ渡す実ペイロード、再送時のPOST一回、結果照合、版・操作履歴を確認する結合テストを追加。
- 既存テストで255文字境界・未翻訳・税計算・失敗再試行・同時送信・結果不明時のGET照合・権限を検証。
- freee HTTPとSecret Managerはモック。FirestoreはローカルEmulator。実際の翻訳サービスと帳票レイアウトは自動テストの対象外。

## ユーザー動作確認
1. テスト用RFQに次の2品目を登録し、翻訳結果の確認・出力文の修正後に明細を確定する。下表の「改行」は実際の改行で入力する。

| 品番 | 原文 | 見積用出力文 | 数量 | 単位 | 販売単価 | 税率 |
| --- | --- | --- | --- | --- | --- | --- |
| A-1 | 六角ボルト | Hex bolt（改行）Stainless steel | 2 | pcs | 100 | 10% |
| 0002-Ω | ワッシャー | Washer Ø10（改行）For M10 bolt | 1.5 | pcs | 80 | 8%（通常） |

2. 2品目目は登録英訳を空欄にし、出力文を手入力する。「翻訳確認」なしで保存できないことを確認し、内容を確認してチェックする。
3. 税別、明細・税とも切り捨て、実在するテスト用freee取引先ID、件名「P3-005 動作確認」を指定して保存。期待値は税抜320円、税29円、合計349円。再読込と保存履歴を確認する。
4. 元RFQの原文・訳文を変更し、保存版の原文・出力文・数量・金額が変わらないことを確認する。
5. freeeへ実登録する場合は画面の確認ダイアログで事業所・取引先・上記明細・349円を照合して登録する。期限切れの場合はfreee連携で再認可後に再試行する。顧客への送付は不要。
6. 作成された帳票を開き、2明細の欠落・文字化け・改行からスペースへの変換・品番の先頭ゼロ・数量・単価・税・合計349円を確認する。API応答一致だけでは帳票表示の確認済みとしない。リンクが返らない場合はfreee画面で作成済みIDを探す。
7. 再読込後も作成済みIDが維持され、追加登録できないこと、保存履歴と操作履歴を確認する。結果不明の場合は新規作成せず既存IDで照合する。

長文境界は別のテストRFQで確認する。品番A-1と区切りスペースの計4文字＋出力文251文字は255文字、252文字なら256文字で保存拒否となる。確定後の出力文は編集できないので別RFQを使用する。

## 完了判定
実帳票の目視結果・見積書ID・確認日・ユーザーOKを受領するまで完了にしない。次の作業はP3-005の帳票確認と完了記録。未定義のP3-006は先取りしない。

## 実行結果（2026-09-14）
- typecheck、lint、フロントproduction build、Functions build: 成功。
- 単体103件、Functions9件、Firestore Emulator126件（追加2件を含む）: 成功。
- buildは依存ライブラリの注釈・既存のチャンクサイズ警告あり。
- 管理シート: ユーザー確認待ち／Codex確認OK／ユーザー未確認。
- テストと資料のみ変更。本番デプロイは不要。実freee登録・帳票目視・ブラウザ一連操作は未実施。

## 取引先ID入力の改善（2026-09-14、上記の手入力手順より優先）
既存見積でHTTP400が発生したため、ユーザー承認により取引先名で選ぶ仕様を追加した。400の原因が取引先IDだけだったとはまだ断定していない。

1. ページを再読込し「freee取引先を選ぶ」を開く。
2. 名前を入力して「検索・再読み込み」。空欄なら一覧を取得する。50件を超える場合は「さらに読み込む」。
3. 対象の取引先を選択し「販売見積を保存」。旧版を残し、新しい版へ名前と内部IDを保存する。
4. 「freeeへの登録内容を確認」でRFQ顧客とfreee取引先名を照合する。
5. 実登録後の帳票確認は既存手順に従う。

取引先コードの利用設定は不要。期限切れはfreee連携画面で再認可。403の場合はfreeeアプリの取引先参照権限と事業所権限を確認する。自動取引先作成・顧客マスター同期・見積の自動登録は追加していない。
自動確認: typecheck・lint・build・Functions build、単体103件、Functions9件、Firestore Emulator130件成功。実API検索と画面操作はユーザー確認待ち。
仕様根拠: https://github.com/freee/freee-api-schema/blob/master/v2020_06_15/open-api-3/api-schema.json （GET /api/1/partners と GET /api/1/partners/{id}）。
- 本番反映完了（2026-09-14）: searchFreeePartners、saveSalesQuotePricing、Hostingを反映。公開先 https://ieppurchase.web.app 。名前選択から保存・登録内容確認をユーザー確認する。

## 保存後のボタン無効化修正（2026-09-14）
保存済み設定と入力設定をJSON文字列の並び順で比較していたため、Firestore読み込み後に未保存と誤判定していた。同じスキーマに正規化し、品目ID順で値を比較するよう修正。未保存の場合は案内を表示する。
確認手順: ページを強制再読込し、保存版2のまま「freeeへの登録内容を確認」を押せること、件名等を変更すると保存を求め、保存後に再び押せることを確認する。見積データの再作成は不要。
typecheck・lint・build・単体106件成功。初回の並列実行は環境メモリ不足で失敗し、単一ワーカーで再実行して成功した。FunctionsとRulesは今回変更していない。

## HTTP400詳細表示（2026-09-14）
正しい取引先を選んだ保存版2でも400が継続。既存実装が拒否理由を捨てていたため、公式のerrors[].messagesを限定して表示するよう修正。原因自体はまだ未確定であり、エラー解消済みとは扱わない。
修正反映後、利用者が同じ保存版で登録を再試行した際の「freeeの詳細」を確認する。取引先や品目を作り直す必要はない。実登録を自動実行する処理は追加しない。API応答の全文と認証情報は保存しない。
自動確認: typecheck・lint・build、単体109件、lint修正後の関連24件成功。
